# 예상 질문 100개 (1/4) - 기술 아키텍처

> 발표 질의응답 대비 - 시스템 구조 및 기술 선택
> 작성일: 2025-11-11

---

## 📋 카테고리별 질문 (1-25)

1. [전체 시스템 구조 (1-8)](#1-전체-시스템-구조)
2. [기술 스택 선택 이유 (9-16)](#2-기술-스택-선택-이유)
3. [데이터베이스 설계 (17-25)](#3-데이터베이스-설계)

---

## 1. 전체 시스템 구조

### Q1. 시스템의 전체 아키텍처를 간단히 설명해주세요.

**답변:**
```
Minute AI는 4개 레이어로 구성됩니다:

1. Frontend: Vanilla JavaScript + HTML5
2. Backend: Flask 3.x (Python)
3. AI Layer: Gemini 2.5 Pro/Flash + OpenAI Embedding
4. Database: SQLite (관계형) + ChromaDB (벡터)

핵심 플로우:
오디오 업로드 → Gemini STT → 스마트 청킹 →
ChromaDB 저장 → RAG 기반 챗봇
```

**코드 위치:** `app.py` (메인 애플리케이션)

---

### Q2. 왜 Flask를 선택했나요? FastAPI나 Django는 고려하지 않았나요?

**답변:**
```
Flask를 선택한 이유:

1. 가볍고 빠른 프로토타이핑
   - 최소한의 보일러플레이트 코드
   - 팀원 모두 Python 경험 있음

2. SSE (Server-Sent Events) 지원 용이
   - 실시간 업로드 진행 상태 전송
   - FastAPI도 가능하지만 Flask가 더 직관적

3. 동시 접속 요구사항
   - 현재: 개인/소규모 팀 (10명 이하)
   - Flask로 충분히 대응 가능
   - 향후 확장 시 FastAPI 마이그레이션 고려

Django는 너무 무거워서 제외했습니다.
```

**참고:** `app.py:334` - SSE 구현 부분

---

### Q3. 프론트엔드를 React나 Vue가 아닌 Vanilla JS로 개발한 이유는?

**답변:**
```
Vanilla JS 선택 이유:

1. 빠른 개발 속도
   - 프로젝트 기간: 2주
   - 번들링/빌드 설정 시간 절약
   - 즉시 개발 시작 가능

2. 가벼운 번들 크기
   - React: 약 140KB (min+gzip)
   - Vanilla JS: 약 10KB
   - 초기 로딩 속도 14배 향상

3. 단순한 UI 요구사항
   - 복잡한 상태 관리 불필요
   - sessionStorage로 충분

향후 대규모 확장 시 React 전환 고려 중입니다.
```

**코드 위치:** `static/js/script.js`, `static/js/viewer.js`

---

### Q4. SSE (Server-Sent Events)를 사용한 이유는? WebSocket은 고려하지 않았나요?

**답변:**
```
SSE 선택 이유:

1. 단방향 통신으로 충분
   - 서버 → 클라이언트 (진행 상태 전송)
   - 클라이언트 → 서버 불필요
   - WebSocket은 양방향이 필요한 채팅 등에 적합

2. 구현 간단
   - Flask: yield로 간단히 구현
   - WebSocket: 별도 라이브러리 필요

3. 자동 재연결
   - SSE는 브라우저가 자동 재연결
   - WebSocket은 수동 구현 필요

4. HTTP 프로토콜 사용
   - 방화벽 통과 용이
   - HTTPS 적용 간단
```

**코드 위치:** `app.py:334` `/upload` 라우트

**구현 예시:**
```python
@app.route('/upload', methods=['POST'])
def upload_and_process():
    def generate():
        yield f"data: {json.dumps({'step': 'upload', 'message': '업로드 중...'})}\n\n"
        # STT 처리...
        yield f"data: {json.dumps({'step': 'stt', 'message': 'STT 중...'})}\n\n"

    return Response(generate(), mimetype='text/event-stream')
```

---

### Q5. 모놀리식 아키텍처인데, 마이크로서비스로 전환할 계획은?

**답변:**
```
현재는 모놀리식이 적합합니다:

장점:
1. 빠른 개발 (2주 내 완성)
2. 간단한 배포 (단일 서버)
3. 트랜잭션 관리 용이 (SQLite + ChromaDB 동시 삭제)

마이크로서비스 전환 고려 시점:
1. DAU 1000명 이상
2. 팀 규모 5명 이상
3. 기능 독립적 확장 필요 (예: STT 서버 분리)

현재 성능:
- 동시 접속: 10명 문제없음
- 업로드 처리: 1건당 평균 2분
- 챗봇 응답: 3.77초

확장 시 분리 우선순위:
1. STT 서비스 (가장 무거움)
2. 챗봇 서비스 (독립적)
3. 파일 스토리지 (S3 분리)
```

**참고:** `next.md` - 향후 개선 계획

---

### Q6. 시스템의 병목 지점은 어디인가요?

**답변:**
```
현재 병목 지점:

1. STT 처리 (가장 느림)
   - Gemini API 호출: 평균 1-2분
   - 해결 방안: 비동기 처리 (Celery + Redis)

2. 벡터 임베딩
   - OpenAI API 호출: 청크당 0.5초
   - 100개 청크: 약 50초
   - 해결 방안: 배치 처리 (이미 구현됨)

3. SQLite 동시 쓰기
   - 동시 업로드 2개 이상 시 Lock
   - 해결 방안: PostgreSQL 전환

병목 아님:
- 챗봇 응답: 3.77초 (만족)
- 벡터 검색: 0.2초 (빠름)
- 파일 다운로드: CDN 없어도 충분
```

**성능 측정:** `test_demo_chatbot.py` - 성능 테스트 코드

---

### Q7. 확장성(Scalability)을 고려한 설계인가요?

**답변:**
```
수평 확장(Horizontal Scaling) 고려 사항:

현재 제약:
1. SQLite: 단일 서버만 가능
   → PostgreSQL 전환 필요

2. 파일 저장: 로컬 uploads/ 폴더
   → S3 + CloudFront 전환 필요

3. 세션: Flask 기본 세션 (메모리)
   → Redis Session Store 필요

확장 시나리오:
- 100명 동시 접속: 현재 구조 가능
- 1000명 동시 접속: DB + 파일 스토리지 분리 필요
- 10000명 동시 접속: 마이크로서비스 전환 필요

준비된 부분:
1. ChromaDB: 클라이언트-서버 모드 지원
2. API 기반 설계: 프론트엔드 분리 용이
3. 환경 변수 관리: .env로 설정 분리
```

---

### Q8. 시스템의 가용성(Availability)은 어떻게 보장하나요?

**답변:**
```
현재 가용성 수준: 99% (개발 환경)

보장 방법:
1. 오류 처리
   - try-except로 모든 API 보호
   - 사용자에게 명확한 에러 메시지

2. 폴백 메커니즘
   - 스마트 청킹 실패 시 RecursiveCharacterTextSplitter 사용
   - SelfQuery 실패 시 Similarity Search로 전환

3. 데이터 백업
   - SQLite: 매일 자동 백업 (권장)
   - ChromaDB: database/vector_db/ 폴더 백업

운영 환경 개선 필요:
1. Health Check 엔드포인트
2. 로그 모니터링 (ELK Stack)
3. 오류 알림 (Slack, Email)
4. 데이터베이스 복제 (Master-Slave)
```

**코드 위치:** `utils/vector_db_manager.py:173` - 폴백 로직

---

## 2. 기술 스택 선택 이유

### Q9. 왜 Gemini를 선택했나요? Whisper나 다른 STT는 고려하지 않았나요?

**답변:**
```
Gemini 2.5 Pro 선택 이유:

1. 화자 분리 (Speaker Diarization) 기본 지원
   - Whisper: 별도 화자 분리 모델 필요 (pyannote.audio)
   - Gemini: enable_speaker_diarization=True만으로 해결

2. 긴 오디오 지원
   - Whisper: 25MB 제한
   - Gemini: 약 2시간 오디오 지원

3. 정확도
   - Gemini: 95% 이상 (테스트 결과)
   - Whisper: 90% 정도

4. 비용
   - Gemini: 무료 할당량 충분
   - Whisper API: 분당 $0.006

단점:
- API 의존성 (인터넷 필요)
- 처리 속도 (1-2분)

향후: Whisper Large v3 + pyannote.audio 조합 고려
```

**코드 위치:** `utils/stt.py:20` `transcribe_audio()`

---

### Q10. ChromaDB를 선택한 이유는? Pinecone이나 Weaviate는?

**답변:**
```
ChromaDB 선택 이유:

1. 로컬 실행 가능
   - 별도 서버 불필요
   - database/vector_db/ 폴더에 저장
   - Pinecone: 클라우드 필수

2. 무료
   - ChromaDB: 완전 무료
   - Pinecone: 무료 플랜 제한적

3. Python 친화적
   - LangChain 완벽 통합
   - pip install chromadb로 간단 설치

4. 충분한 성능
   - 1만 개 문서: 0.2초 검색
   - 현재 요구사항: 100개 문서

단점:
- 대규모 확장 시 성능 저하 가능
- 분산 처리 미지원

Pinecone 전환 시점:
- 문서 100만 개 이상
- 글로벌 서비스 (지역별 분산)
```

**코드 위치:** `utils/vector_db_manager.py:27` - ChromaDB 초기화

---

### Q11. OpenAI Embedding을 사용하는 이유는? Gemini Embedding은?

**답변:**
```
OpenAI text-embedding-3-small 선택 이유:

1. 검증된 성능
   - MTEB 벤치마크 상위권
   - 1536차원 (적절한 크기)

2. LangChain 기본 지원
   - from langchain_openai import OpenAIEmbeddings
   - 별도 커스터마이징 불필요

3. 비용 효율
   - $0.00002 / 1K 토큰
   - 1000개 청크: 약 $0.2

Gemini Embedding 미사용 이유:
- LangChain 통합 미흡
- 문서 부족

향후 고려:
- Gemini Embedding 안정화 시 전환 검토
- 비용 절감 가능성
```

**코드 위치:** `utils/vector_db_manager.py:32` `self.embedding_function`

---

### Q12. SQLite를 선택한 이유는? PostgreSQL이나 MySQL은?

**답변:**
```
SQLite 선택 이유:

1. 간단한 설정
   - 별도 서버 불필요
   - 파일 기반 (minute_ai.db)
   - 즉시 개발 시작 가능

2. 충분한 성능
   - 현재 사용량: 동시 접속 10명 이하
   - 읽기 성능: PostgreSQL과 유사
   - 트랜잭션 지원

3. 백업 간편
   - 단일 파일 복사로 백업
   - 버전 관리 용이

단점:
- 동시 쓰기 제한 (Lock)
- 네트워크 접근 불가

PostgreSQL 전환 시점:
- 동시 업로드 10건 이상
- 분산 서버 필요 시
- 복잡한 쿼리 필요 시

마이그레이션 준비:
- db_manager.py로 DB 로직 분리
- ORM(SQLAlchemy) 전환 고려
```

**코드 위치:** `utils/db_manager.py` - 모든 DB 작업

---

### Q13. Firebase 인증을 선택한 이유는?

**답변:**
```
Firebase Authentication 선택 이유:

1. Google OAuth 간편 통합
   - 버튼 클릭 한 번으로 로그인
   - 프로필 사진 자동 제공

2. 보안
   - ID Token 검증 (서버 사이드)
   - 만료 시간 자동 관리
   - HTTPS 필수

3. 무료
   - 월 5만 사용자까지 무료
   - 현재 요구사항: 10명

4. 빠른 구현
   - Firebase Admin SDK
   - 2시간 내 구현 완료

대안 고려:
- JWT 자체 구현: 보안 리스크
- Auth0: 유료
- Keycloak: 설정 복잡

단점:
- Firebase 종속성
- 오프라인 불가
```

**코드 위치:** `utils/firebase_auth.py`

---

### Q14. ffmpeg를 사용하는 이유는? 다른 방법은?

**답변:**
```
ffmpeg 사용 이유:

1. MP4 비디오 → 오디오 추출
   - 사용자가 비디오 업로드 시 자동 변환
   - 화질 손실 없이 오디오만 추출

2. 무료 오픈소스
   - 라이선스 문제 없음
   - 대부분 OS에 설치 가능

3. 고성능
   - 멀티스레딩 지원
   - 10분 비디오: 10초 내 변환

명령어:
ffmpeg -i input.mp4 -vn -acodec libmp3lame output.mp3

대안:
- MoviePy: Python 라이브러리 (느림)
- Pydub: 간단하지만 ffmpeg 의존

단점:
- 서버에 ffmpeg 설치 필요
- GCP 배포 시 Dockerfile에 포함 필요
```

**코드 위치:** `utils/stt.py:91` - MP4 변환 로직

---

### Q15. Chart.js를 선택한 이유는?

**답변:**
```
Chart.js 선택 이유 (화자 점유율 차트):

1. 간단한 사용법
   - CDN으로 즉시 사용
   - 5줄 코드로 차트 생성

2. 반응형
   - 모바일/데스크톱 자동 대응
   - Canvas 기반 렌더링

3. 무료
   - MIT 라이선스
   - 상업적 사용 가능

대안:
- D3.js: 너무 복잡
- Recharts: React 전용
- ApexCharts: 유료 기능 많음

사용 예시:
new Chart(ctx, {
    type: 'doughnut',
    data: {
        labels: ['Speaker 1', 'Speaker 2'],
        datasets: [{data: [65.3, 34.7]}]
    }
});
```

**코드 위치:** `templates/viewer.html` - Chart.js 스크립트

---

### Q16. 왜 프레임워크(React, Vue)를 사용하지 않았나요?

**답변:**
```
프레임워크 미사용 이유:

1. 프로젝트 규모
   - 페이지 수: 5개
   - 컴포넌트 재사용: 거의 없음
   - 복잡한 상태 관리: 불필요

2. 개발 속도
   - 번들링 설정: 1-2일 소요
   - Vanilla JS: 즉시 개발 시작
   - 프로젝트 기간: 2주

3. 성능
   - 초기 로딩: 14배 빠름
   - 런타임 오버헤드: 없음

4. 팀 역량
   - 모두 JavaScript 기본 가능
   - React 학습 곡선 고려

Vanilla JS로 충분한 이유:
- sessionStorage로 상태 관리
- Jinja2 템플릿으로 컴포넌트 재사용
- 이벤트 리스너로 인터랙션 처리

향후 전환 고려:
- 페이지 20개 이상
- 복잡한 상태 관리 필요
- 팀 규모 5명 이상
```

**코드 위치:** `static/js/script.js`, `static/js/viewer.js`

---

## 3. 데이터베이스 설계

### Q17. 왜 SQLite와 ChromaDB를 함께 사용하나요?

**답변:**
```
하이브리드 DB 구조 이유:

SQLite (관계형):
- 용도: 메타데이터, 사용자 정보, 권한
- 장점: ACID 트랜잭션, 정확한 조회
- 예: 제목, 날짜, owner_id, 공유 관계

ChromaDB (벡터):
- 용도: 대화 내용, 요약 (의미론적 검색)
- 장점: 코사인 유사도 검색, RAG
- 예: "프로젝트 일정 논의한 부분" 검색

역할 분리:
- SQLite: WHERE meeting_id = '123' (정확 매칭)
- ChromaDB: 유사한 의미의 문서 검색 (근사 매칭)

같이 쓰는 이유:
- 벡터 DB만으로는 메타데이터 관리 어려움
- 관계형 DB만으로는 의미론적 검색 불가

동기화:
- meeting_id로 연결
- 삭제 시 양쪽 모두 삭제 (트랜잭션 유사)
```

**코드 위치:**
- SQLite: `utils/db_manager.py`
- ChromaDB: `utils/vector_db_manager.py`
- 동시 삭제: `utils/vector_db_manager.py:658` `_delete_all_meeting_data()`

---

### Q18. meeting_id는 어떻게 생성하나요? UUID를 사용하는 이유는?

**답변:**
```
meeting_id 생성 방식:

형식: "meeting_" + UUID4 (8자리)
예: "meeting_a3f7b2c1"

UUID 사용 이유:

1. 충돌 방지
   - UUID4: 2^122 가지 (충돌 확률 거의 0)
   - Auto Increment: 서버 분산 시 충돌 가능

2. 보안
   - 예측 불가능
   - Auto Increment: meeting_id=1, 2, 3... 순차 접근 가능

3. 분산 시스템 대응
   - 서버 간 ID 조율 불필요
   - 각 서버가 독립적으로 생성 가능

생성 코드:
import uuid
meeting_id = f"meeting_{uuid.uuid4().hex[:8]}"

단점:
- 가독성 낮음 (vs Auto Increment)
- 인덱스 크기 약간 증가

대안:
- ULID: 시간순 정렬 가능 (향후 고려)
```

**코드 위치:** `app.py:392` - meeting_id 생성

---

### Q19. 왜 owner_id를 meeting_dialogues와 meeting_minutes 양쪽에 저장하나요?

**답변:**
```
owner_id 중복 저장 이유:

1. 비정규화 (Denormalization)
   - 조회 성능 향상
   - JOIN 없이 owner_id 확인 가능

2. 데이터 일관성
   - meeting_dialogues: STT 결과 (필수)
   - meeting_minutes: 요약 (선택, 나중에 생성 가능)
   - 둘 중 하나만 있어도 owner_id 확인 가능

3. 권한 체크 간소화
   - can_access_meeting(): meeting_dialogues만 조회
   - meeting_minutes 생성 전에도 권한 체크 가능

예시:
-- meeting_dialogues에 owner_id 있음
SELECT owner_id FROM meeting_dialogues WHERE meeting_id = 'xxx' LIMIT 1;

-- meeting_minutes 생성 전에도 권한 확인 가능
```

**코드 위치:** `utils/user_manager.py:138` `can_access_meeting()`

**트레이드오프:**
- 장점: 빠른 조회, 간단한 로직
- 단점: 저장 공간 약간 증가, 업데이트 시 양쪽 수정 필요

---

### Q20. meeting_shares 테이블의 permission 필드는 왜 필요한가요? 현재 'read'만 있는데?

**답변:**
```
permission 필드 설계 의도:

현재 구현:
- 'read': 읽기 전용 (공유받은 사람)

향후 확장 가능성:
- 'write': 수정 권한 (공동 편집)
- 'comment': 댓글만 가능
- 'admin': 삭제 권한

확장형 설계 (Future-Proof):
- 지금은 'read'만 사용
- 코드 변경 없이 기능 추가 가능

사용 예시:
CREATE TABLE meeting_shares (
    ...
    permission TEXT DEFAULT 'read',  -- 'read', 'write', 'admin'
    ...
);

-- 향후 수정 권한 체크
SELECT permission FROM meeting_shares
WHERE meeting_id = 'xxx' AND shared_with_user_id = 1;

if permission in ['write', 'admin']:
    allow_edit()
```

**코드 위치:** `utils/user_manager.py:276` `share_meeting()`

**현재 권한 체계:**
- Owner: 읽기, 수정, 삭제, 공유
- Shared User: 읽기만 가능
- Admin: 모든 권한

---

### Q21. ChromaDB의 메타데이터 구조를 설명해주세요.

**답변:**
```
meeting_chunks 메타데이터:

{
    "meeting_id": "meeting_a3f7b2c1",       # 회의 고유 ID
    "dialogue_id": "meeting_a3f7b2c1_chunk_0",  # 청크 고유 ID
    "chunk_index": 0,                       # 청크 순서 (정렬용)
    "title": "사자회담 회의록",             # 회의 제목
    "meeting_date": "2025-11-07 14:30:00",  # 회의 날짜
    "audio_file": "a3f7b2c1-...-4n5o.mp3",  # 원본 파일명
    "start_time": 0.0,                      # 청크 시작 시간 (초)
    "end_time": 125.3,                      # 청크 종료 시간 (초)
    "speaker_count": 2                      # 화자 수
}

meeting_subtopic 메타데이터:

{
    "meeting_id": "meeting_a3f7b2c1",
    "meeting_title": "사자회담 회의록",
    "meeting_date": "2025-11-07 14:30:00",
    "audio_file": "a3f7b2c1-...-4n5o.mp3",
    "main_topic": "프로젝트 일정 조율",     # 소주제명
    "summary_index": 0                      # 요약 순서
}

메타데이터 활용:
1. 필터링: meeting_id로 특정 회의만 검색
2. 정렬: chunk_index, summary_index로 순서 보장
3. 타임스탬프: start_time, end_time으로 비디오 점프
4. 출처 표시: title, meeting_date를 챗봇 답변에 포함
```

**코드 위치:** `utils/vector_db_manager.py:151` - 메타데이터 생성

---

### Q22. 왜 meeting_date를 TEXT로 저장하나요? DATETIME이나 INTEGER(UNIX)는?

**답변:**
```
TEXT 형식 선택 이유:

형식: "YYYY-MM-DD HH:MM:SS"
예: "2025-11-07 14:30:00"

장점:
1. 가독성
   - DB 직접 조회 시 한눈에 파악
   - 디버깅 용이

2. ChromaDB 호환성
   - ChromaDB는 문자열 메타데이터만 지원
   - DATETIME 타입 미지원

3. 간단한 비교
   - SQLite: WHERE meeting_date > '2025-11-01'
   - 문자열 사전순 정렬 = 날짜순 정렬

4. 타임존 명시적 관리
   - 현재: 서버 로컬 시간
   - 향후: 'YYYY-MM-DD HH:MM:SS+09:00' 추가 가능

단점:
- 날짜 연산 불편 (DATETIME 변환 필요)
- 저장 공간 약간 증가

대안:
- UNIX Timestamp: 1699329000 (숫자, 작음)
- ISO 8601: "2025-11-07T14:30:00Z" (타임존 포함)
```

**코드 위치:** `utils/validation.py:34` `parse_meeting_date()`

---

### Q23. users 테이블에 google_id와 email이 둘 다 UNIQUE인 이유는?

**답변:**
```
이중 UNIQUE 제약 이유:

1. google_id UNIQUE:
   - Firebase UID (고유 식별자)
   - 동일 Google 계정 중복 가입 방지

2. email UNIQUE:
   - 공유 기능에서 이메일로 사용자 검색
   - 한 이메일 = 한 계정

마이그레이션 시나리오:
1. Firebase 도입 전: 이메일만 저장 (google_id = NULL)
2. Firebase 로그인 시: google_id 업데이트
3. 중복 방지: 같은 이메일로 여러 계정 생성 불가

예외 처리:
-- google_id 없는 더미 계정
INSERT INTO users (email, google_id) VALUES ('user@ex.com', NULL);

-- Firebase 로그인 시 업데이트
UPDATE users SET google_id = 'firebase_123' WHERE email = 'user@ex.com';

-- 이후 로그인: google_id로 조회
SELECT * FROM users WHERE google_id = 'firebase_123';
```

**코드 위치:** `utils/user_manager.py:25` `get_or_create_user()`

**스키마:**
```sql
CREATE TABLE users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    google_id TEXT UNIQUE,        -- Firebase UID (마이그레이션 대비 NULL 허용)
    email TEXT UNIQUE NOT NULL,   -- 이메일 (필수)
    ...
);
```

---

### Q24. ChromaDB에서 삭제 시 어떻게 검증하나요?

**답변:**
```
삭제 검증 프로세스 (3단계):

1. 삭제 전 개수 확인
collection.get(where={"meeting_id": meeting_id})
before_count = len(results['ids'])
print(f"[삭제 전] {before_count}개")

2. 삭제 실행
collection.delete(where={"meeting_id": meeting_id})
print(f"[삭제 수행] {before_count}개 삭제 시도")

3. 삭제 후 개수 확인
after_results = collection.get(where={"meeting_id": meeting_id})
after_count = len(after_results['ids'])

if after_count == 0:
    print("✅ 삭제 검증 성공")
else:
    print(f"⚠️ 삭제 검증 실패: {after_count}개 남음")

로그 예시:
📊 [Vector DB Chunks 삭제 검증 시작] meeting_id = meeting_xxx
======================================================================
[삭제 전] meeting_chunk: 15개
----------------------------------------------------------------------
[삭제 수행] meeting_chunk: 15개 삭제 시도
----------------------------------------------------------------------
[삭제 후] meeting_chunk: 0개 남음
✅ Vector DB (meeting_chunk) 삭제 검증 성공: 모든 데이터가 삭제되었습니다.
======================================================================
```

**코드 위치:** `utils/vector_db_manager.py:711` - chunks 삭제 검증
**코드 위치:** `utils/vector_db_manager.py:756` - subtopic 삭제 검증

**중요성:**
- ChromaDB delete()는 반환값 없음
- 삭제 실패 시 고아 데이터 발생 가능
- 로그로 수동 확인 필요

---

### Q25. 데이터베이스 마이그레이션 전략은?

**답변:**
```
현재 마이그레이션 도구: migrate_db.py

마이그레이션 시나리오:

1. 스키마 변경 (예: 컬럼 추가)
   - ALTER TABLE users ADD COLUMN phone TEXT;
   - 기존 데이터 유지

2. Firebase 인증 추가 (2025-11-06)
   - google_id 컬럼 추가
   - 기존 사용자: google_id = NULL → 첫 로그인 시 업데이트

3. ChromaDB 메타데이터 변경
   - 기존: title 필드 없음
   - 신규: title 필드 추가
   - 방법: 전체 재임베딩 (비용 큼)

향후 개선:

1. Alembic 도입 (SQLite → PostgreSQL 전환 시)
   - 버전 관리
   - 롤백 기능
   - 팀 협업 용이

2. 데이터 검증
   - 마이그레이션 전후 row count 확인
   - 중요 데이터 샘플링 검증

3. 블루-그린 배포
   - 구 버전 DB 유지
   - 신 버전 DB 테스트 후 전환
   - 문제 시 즉시 롤백
```

**코드 위치:** `migrate_db.py` - 마이그레이션 스크립트

**마이그레이션 체크리스트:**
```bash
1. DB 백업
cp database/minute_ai.db database/minute_ai.db.backup

2. 마이그레이션 실행
python migrate_db.py

3. 검증
sqlite3 database/minute_ai.db "SELECT COUNT(*) FROM users;"

4. 문제 발생 시 롤백
mv database/minute_ai.db.backup database/minute_ai.db
```

---

## 📌 핵심 요약

**아키텍처:**
- 4레이어 구조 (Frontend, Backend, AI, Database)
- 모놀리식 아키텍처 (현재 규모에 적합)
- SSE로 실시간 진행 상태

**기술 선택:**
- Gemini: 화자 분리 + 긴 오디오 지원
- ChromaDB: 로컬 실행 + 무료
- SQLite: 간단한 설정 + 충분한 성능
- Firebase: Google OAuth 간편 통합

**DB 설계:**
- SQLite + ChromaDB 하이브리드
- meeting_id로 양방향 연결
- owner_id로 권한 관리
- 삭제 검증 로그로 데이터 무결성 보장

---

**다음 파일:** `03_예상_질문_02_STT_및_AI.md`
