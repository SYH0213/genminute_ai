# 예상 질문 100개 (4/4) - 보안 및 배포

> 발표 질의응답 대비 - 보안, 권한, GCP 배포, 운영
> 작성일: 2025-11-11

---

## 1. 보안 (Q76-85)

### Q76. 주요 보안 위협은?
**OWASP Top 10 대응:**
1. SQL Injection: Parameterized Query 사용
2. XSS: Jinja2 자동 이스케이프
3. CSRF: SameSite Cookie (향후 Flask-WTF)
4. 파일 업로드: 확장자 검증, UUID 파일명

### Q77. API 키 관리는?
**.env 파일 (gitignore):**
```bash
GOOGLE_API_KEY=AIza...
OPENAI_API_KEY=sk-proj-...
```
**운영:** AWS Secrets Manager / GCP Secret Manager

### Q78. 사용자 인증 보안은?
**Firebase ID Token:**
- 만료 시간: 1시간
- 서버 사이드 검증 필수
- HTTPS 필수 (토큰 노출 방지)
**코드:** `utils/firebase_auth.py:43` `verify_id_token()`

### Q79. 파일 접근 제어는?
```python
@app.route('/uploads/<path:filename>')
@login_required
def serve_file(filename):
    # 권한 체크
    meeting_id = get_meeting_id_from_filename(filename)
    if not can_access_meeting(session['user_id'], meeting_id):
        abort(403)
    
    return send_from_directory('uploads', filename)
```

### Q80. HTTPS는 어떻게 적용하나요?
**개발:** HTTP (localhost)
**운영:** GCP Load Balancer SSL 자동 적용
```yaml
# app.yaml
handlers:
  - url: /.*
    secure: always
    script: auto
```

### Q81. 비밀번호 저장은?
**Firebase 관리:**
- 비밀번호 저장 안 함 (Google OAuth)
- Firebase가 해시 관리
**직접 구현 시:** bcrypt 사용 권장

### Q82. 세션 하이재킹 방지는?
**Flask Session:**
- SECRET_KEY로 서명
- HttpOnly Cookie
- SameSite=Lax
**운영:** HTTPS + Secure Cookie

### Q83. DDoS 방지는?
**현재:** 없음
**GCP 배포 시:**
- Cloud Armor (WAF)
- Rate Limiting
```python
from flask_limiter import Limiter

limiter = Limiter(app, default_limits=["100 per hour"])
```

### Q84. 데이터 암호화는?
**저장 (at-rest):**
- SQLite: 파일 시스템 암호화 (OS 레벨)
- 향후: SQLCipher (DB 암호화)

**전송 (in-transit):**
- HTTPS (TLS 1.3)
- API 키 헤더 전송

### Q85. 로그 보안은?
**민감 정보 마스킹:**
```python
print(f"User {user_id} logged in")  # OK
print(f"API Key: {api_key[:4]}****")  # 마스킹
```
**로그 레벨:** INFO (운영), DEBUG (개발)

---

## 2. 권한 관리 (Q86-90)

### Q86. Admin과 User 차이는?
**Admin (.env ADMIN_EMAILS):**
- 모든 노트 접근
- Debug 메뉴 (파이프라인 테스트)
- 사용자 관리 (향후)

**User:**
- 본인 노트 + 공유받은 노트만
- 공유받은 노트: 읽기만 가능

### Q87. 공유 권한 종류는?
**현재:** 'read' (읽기만)
**향후:**
- 'write': 수정 권한
- 'comment': 댓글만
- 'admin': 삭제 권한

### Q88. 노트 삭제 권한은?
**삭제 가능:**
- Owner (본인이 생성한 노트)
- Admin (모든 노트)

**삭제 불가:**
- 공유받은 사람

### Q89. 공유 취소는?
```python
DELETE FROM meeting_shares
WHERE meeting_id = ? AND shared_with_user_id = ?
```
**UI:** 공유 목록 → X 버튼

### Q90. 팀 단위 권한은?
**향후 구현:**
- organizations 테이블
- team_members 테이블
- 팀 내 노트 자동 공유

---

## 3. GCP 배포 (Q91-95)

### Q91. GCP App Engine 배포 방법은?
**배포 가이드:** `markdown_folder/GCP_배포_가이드.md`
```bash
# 1. gcloud 설치 및 로그인
gcloud init

# 2. app.yaml 작성
runtime: python39
entrypoint: gunicorn -b :$PORT app:app

# 3. 배포
gcloud app deploy

# 4. 확인
gcloud app browse
```

### Q92. 환경 변수 설정은?
**app.yaml:**
```yaml
env_variables:
  GOOGLE_API_KEY: "AIza..."
  OPENAI_API_KEY: "sk-proj-..."
```
**보안:** Secret Manager 사용 권장
```yaml
env_variables:
  GOOGLE_API_KEY: "secret-ref://google-api-key"
```

### Q93. 데이터베이스는 어떻게 배포하나요?
**SQLite 문제:**
- App Engine: 로컬 파일 시스템 읽기 전용
- 해결: Cloud SQL (PostgreSQL)

**마이그레이션:**
```bash
# 1. Cloud SQL 인스턴스 생성
gcloud sql instances create minute-ai-db --tier=db-f1-micro

# 2. SQLite → PostgreSQL
pgloader database/minute_ai.db postgresql://...
```

**ChromaDB:**
- 클라이언트-서버 모드로 전환
- 별도 GCE 인스턴스에 ChromaDB 서버

### Q94. 비용은 얼마나 드나요?
**GCP App Engine (사용자 10명):**
- F1 인스턴스: $25/월
- Cloud SQL: $10/월
- Storage: $1/월
- 총: 약 $40/월

**확장 시 (사용자 1000명):**
- F4 인스턴스 × 2: $400/월
- Cloud SQL (HA): $100/월
- 총: 약 $500/월

### Q95. CI/CD는?
**향후 구현:**
```yaml
# .github/workflows/deploy.yml
name: Deploy to GCP

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Deploy
        run: gcloud app deploy
```

---

## 4. 운영 및 모니터링 (Q96-100)

### Q96. 로그 모니터링은?
**GCP Logging:**
```python
import google.cloud.logging

client = google.cloud.logging.Client()
client.setup_logging()

# 자동으로 GCP Logs로 전송
print("User logged in")
```

**로그 쿼리:**
```
resource.type="gae_app"
severity="ERROR"
```

### Q97. 성능 모니터링은?
**GCP Monitoring:**
- CPU, 메모리 사용률
- 응답 시간
- 오류율

**알림 설정:**
- 오류율 5% 이상 → Email/Slack
- 응답 시간 10초 이상 → 알림

### Q98. 데이터베이스 백업은?
**Cloud SQL 자동 백업:**
```bash
# 매일 자동 백업 (7일 보관)
gcloud sql backups create --instance=minute-ai-db

# 복원
gcloud sql backups restore BACKUP_ID --instance=minute-ai-db
```

**ChromaDB:**
```bash
# GCS로 백업
gsutil -m cp -r database/vector_db gs://minute-ai-backup/
```

### Q99. 장애 대응은?
**장애 시나리오:**
1. API 키 만료 → Secret Manager 갱신
2. DB 연결 실패 → Cloud SQL 재시작
3. 메모리 부족 → 인스턴스 스케일업

**Health Check:**
```python
@app.route('/health')
def health():
    # DB 연결 확인
    # API 키 유효성 확인
    return {"status": "healthy"}
```

### Q100. 확장 계획은?
**Phase 1 (현재):**
- 사용자 10명
- 모놀리식
- SQLite + ChromaDB

**Phase 2 (100명):**
- PostgreSQL 전환
- Redis 캐싱
- 비동기 STT (Celery)

**Phase 3 (1000명):**
- 마이크로서비스 분리
- Kubernetes
- Pinecone (벡터 DB)
- S3 + CloudFront

**Phase 4 (10000명):**
- 다중 리전 배포
- Auto Scaling
- CDN 글로벌 배포

---

## 📌 전체 요약

**보안:**
- Firebase Auth, HTTPS, Parameterized Query
- API 키 환경 변수 관리
- 권한 체계 (Admin/Owner/Shared)

**배포:**
- GCP App Engine (Python 3.9)
- Cloud SQL (PostgreSQL)
- Secret Manager (API 키)
- $40/월 (10명 기준)

**운영:**
- GCP Logging/Monitoring
- 자동 백업 (7일)
- Health Check
- CI/CD (GitHub Actions)

**확장:**
- Phase 1: 모놀리식 (현재)
- Phase 2: PostgreSQL + Redis
- Phase 3: 마이크로서비스
- Phase 4: 글로벌 배포

---

## 🎯 발표 마무리 답변 템플릿

**Q: 프로젝트의 가장 큰 성과는?**
```
1. 챗봇 100% 정확도 달성 (20/20 질문)
2. 평균 응답 속도 3.77초 (목표 5초 달성)
3. 완전한 RAG 파이프라인 구현
4. Firebase 인증 + 권한 관리 완료
5. GCP 배포 가이드 작성 완료
```

**Q: 가장 어려웠던 점은?**
```
1. 스마트 청킹 알고리즘 설계
   → 화자/시간/크기 3가지 조건 조합
2. 챗봇 할루시네이션 방지
   → 엄격한 프롬프트 + RAG
3. ChromaDB 삭제 검증
   → 상세 로그로 무결성 보장
```

**Q: 향후 개선 계획은?**
```
단기 (1개월):
- 일괄 삭제 프로그레스 모달
- 챗봇 추천 질문 기능
- STT 결과 수정 기능

중기 (3개월):
- PostgreSQL 전환
- Redis 캐싱
- 비동기 처리 (Celery)

장기 (6개월):
- 다국어 지원
- 실시간 회의 STT
- 모바일 앱
```

---

**작성자:** Claude Code
**최종 수정:** 2025-11-11
**총 질문 개수:** 100개 (25개 × 4파일)
